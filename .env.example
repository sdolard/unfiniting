# Example environment variables for Codex CLI
# Copy this file to `.env` and fill in your secrets.
# Do NOT commit real keys. `.env` is ignored by .gitignore.

# OpenAI API key used by Codex CLI when referenced in ~/.codex/config.toml
# Usage (POSIX shells):
#   set -a; source .env; set +a
# Or export in your shell profile: export CODEX_OPENAI_API_KEY=...
CODEX_OPENAI_API_KEY=

# Note: The default model is set in `~/.codex/config.toml`.
# Example in config.toml:
# [defaults]
# model = "openai/gpt-5-mini-2025-08-07"

